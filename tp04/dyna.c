#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char * miroir(const char *s)
{
    char *nouvelleChaine = (char*) malloc(strlen(s) * sizeof(char));
    for(int i = strlen(s) ; i >= 0 ; i--)
    {

    }
}